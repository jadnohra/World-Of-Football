#include "WENPVolumeCollider.h"

namespace WE {

/*
bool NPVolumeCollider::intersects_sweptContact(const float& sphereRadius, const Vector3& startPos, const Vector3& posDiff, Point& staticContactPoint, float& sweptFraction, SweptContactInfo& contactInfo) {

	switch (mVolume.type) {
		case V_OBB:
			{

			}
			break;
	}

	assrt(false);
	return false;
}
*/

}