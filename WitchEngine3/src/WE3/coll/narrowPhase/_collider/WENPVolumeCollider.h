#ifndef _WENPVolumeCollider_h
#define _WENPVolumeCollider_h


namespace WE {

	/*
	class NPVolumeCollider {
	public:

		bool intersects_sweptContact(const float& sphereRadius, const Vector3& startPos, const Vector3& posDiff, Point& staticContactPoint, float& sweptFraction, SweptContactInfo& contactInfo);

	protected:

		SoftPtr<VolumeStruct> mVolume;
	};
	*/

}

#endif