#ifndef _WEDataPathChain_h
#define _WEDataPathChain_h

#include "../WERef.h"
#include "WEDataPath.h"

namespace WE {

	/*
	class DataPathChain : public Ref {
	protected:

		DataPathChain* mpPrev;
		DataPath::PartIndex mPrevPartCount;
		DataPath mPath;

	public:

		DataPathChain(DataPathChain* pPrev, const TCHAR* path);
		virtual ~DataPathChain();

		DataPath::PartIndex getPartCount() const;
		String* getPart(DataPath::PartIndex partIndex) const;
	};
	*/
}

#endif